# 2026-06-04 Daily Notes

## Spa Automation — COMPLETE ✅

All 17 scenarios from the comprehensive review are now implemented and verified.

### What was done today
- **Scenario E (Late Event) Telegram message** — implemented `sendHeatingStartedLateAlert(lateByMin, estimatedReadyMs)` in `telegram.js`. Fires in Phase 2 when `nowMs > preheatStartMs`, sends: *"Heating started X min late. Estimated ready time: HH:MM AM/PM ET."* Uses current spa temp to estimate ~2 min/degree to 102°F.
- **Commit `270ee45`**: telegram.js late alert + scheduler.js Phase 2 late detection + TEST_PLAN.md update

### Full spa bug fixes (all committed)
| Bug | Fix | Commit |
|---|---|---|
| B1: CALENDAR_LOOK_AHEAD_DAYS=2 | Changed to 7 in scheduler.js:44 | b6b0bcc |
| B2/B3: resolveWeatherCheckMs hardcoded 600s | Now reads cfg.weatherCheckLeadMin | b6b0bcc |
| B4: sendValveFailureAlert only in Phase 2 | Added to Phase 4 failure path too | b6b0bcc |
| B5: failedPreheat flag | Preserves nextSpaEvent + nextSpaEventEndMs on valve failure | b6b0bcc |
| B6: Phase 3 didn't handle failedPreheat | Added phase === 'idle' && failedPreheat path | b6b0bcc |
| B7: Option A approval expiry | expireApprovalDefaultYes() + proceedToHeating flag | b6b0bcc |
| B8: Option B already-approved | Phase 4 checks status=approved, proceeds to heating | b6b0bcc |
| B9: No lock file | Added data/spa-scheduler.lock with PID + stale detection | b6b0bcc |
| B10: nextSpaEventEndMs not persisted | Explicitly persisted in Phase 2 saveState | b6b0bcc |
| E1: resolveWeatherCheckMs ignored weatherCheckLeadMin | Now uses cfg.weatherCheckLeadMin | 88bf432 |
| A4: stale approval not cleared | Phase 1 clears pending approval on new uid | 88bf432 |
| P1: waitForValveReady crashes on Hubitat timeout | Wrapped readSnapshot in try/catch | 88bf432 |
| M1/M2: spaHeatStop not idempotent | Both Phase 3 stop paths check valveState === 'pool' first | 88bf432 |
| C2/C3: calendar monitoring during heating | Phase 3 re-fetches calendar each run, handles remove + end-time change | 88bf432 |
| D1: approvalMatchesContext weak uid check | Strict uid boundary check in approval.js | 88bf432 |

### Agent routing documentation fixed
- AGENTS.md: sessions_spawn now documents `agentId` as required param, not taskName
- MEMORY.md: sessions_spawn now documents `agentId` as required, correct agent table
- Commits `6aeafdb` (AGENTS.md) and `43ecb78` (MEMORY.md)

### Key design decisions captured
- Option A for approval expiry: 5-min silence → auto-approve → proceed to heating
- Weather check fires at preheatStart - weatherCheckLeadMin (config-driven, default 30 min)
- T1: late event starts heating late (acceptable), sends Telegram "late" message (now implemented)
- Valve polling: 30s intervals, max 10-min total wait, 60s retry pause

### Remaining
Nothing — all 17 scenarios implemented and verified (13 PASS, 4 ACCEPTABLE, 0 FAIL).

## Heartbeat
- No actionable items pending.
# 2026-06-04 Daily Notes

## Spa Automation — COMPLETE ✅

All 17 scenarios from the comprehensive review are now implemented and verified.

### What was done today
- **Scenario E (Late Event) Telegram message** — implemented `sendHeatingStartedLateAlert(lateByMin, estimatedReadyMs)` in `telegram.js`. Fires in Phase 2 when `nowMs > preheatStartMs`, sends: *"Heating started X min late. Estimated ready time: HH:MM AM/PM ET."* Uses current spa temp to estimate ~2 min/degree to 102°F.
- **Commit `270ee45`**: telegram.js late alert + scheduler.js Phase 2 late detection + TEST_PLAN.md update

### Full spa bug fixes (all committed)
| Bug | Fix | Commit |
|---|---|---|
| B1: CALENDAR_LOOK_AHEAD_DAYS=2 | Changed to 7 in scheduler.js:44 | b6b0bcc |
| B2/B3: resolveWeatherCheckMs hardcoded 600s | Now reads cfg.weatherCheckLeadMin | b6b0bcc |
| B4: sendValveFailureAlert only in Phase 2 | Added to Phase 4 failure path too | b6b0bcc |
| B5: failedPreheat flag | Preserves nextSpaEvent + nextSpaEventEndMs on valve failure | b6b0bcc |
| B6: Phase 3 didn't handle failedPreheat | Added phase === 'idle' && failedPreheat path | b6b0bcc |
| B7: Option A approval expiry | expireApprovalDefaultYes() + proceedToHeating flag | b6b0bcc |
| B8: Option B already-approved | Phase 4 checks status=approved, proceeds to heating | b6b0bcc |
| B9: No lock file | Added data/spa-scheduler.lock with PID + stale detection | b6b0bcc |
| B10: nextSpaEventEndMs not persisted | Explicitly persisted in Phase 2 saveState | b6b0bcc |
| E1: resolveWeatherCheckMs ignored weatherCheckLeadMin | Now uses cfg.weatherCheckLeadMin | 88bf432 |
| A4: stale approval not cleared | Phase 1 clears pending approval on new uid | 88bf432 |
| P1: waitForValveReady crashes on Hubitat timeout | Wrapped readSnapshot in try/catch | 88bf432 |
| M1/M2: spaHeatStop not idempotent | Both Phase 3 stop paths check valveState === 'pool' first | 88bf432 |
| C2/C3: calendar monitoring during heating | Phase 3 re-fetches calendar each run, handles remove + end-time change | 88bf432 |
| D1: approvalMatchesContext weak uid check | Strict uid boundary check in approval.js | 88bf432 |

### Live session bug discoveries (June 4 evening)
| Bug | Fix | Commit |
|---|---|---|
| B-V1: waitForValveReady checked `=== 'spa'` | Added `\|\| === 'on'` — Hubitat returns "on" in spa mode | c2993d0 |
| B-V2: line 553 `=== 'pool'` | Added `\|\| === 'off'` — Hubitat returns "off" in pool mode | 7d1eea2 |
| B-V3: line 577 `=== 'pool'` | Same fix | 7d1eea2 |
| B-V4: line 631 `=== 'pool'` | Same fix | 7d1eea2 |

### Hubitat device string mapping (learned live)
| Context | Expected string | Actual Hubitat value |
|---|---|---|
| Spa mode | `'spa'` or `'on'` | `'on'` |
| Pool mode | `'pool'` or `'off'` | `'off'` |

### Agent routing documentation fixed
- AGENTS.md: sessions_spawn now documents `agentId` as required param, not taskName
- MEMORY.md: sessions_spawn now documents `agentId` as required, correct agent table
- Commits `6aeafdb` (AGENTS.md) and `43ecb78` (MEMORY.md)

### Key design decisions captured
- Option A for approval expiry: 5-min silence → auto-approve → proceed to heating
- Weather check fires at preheatStart - weatherCheckLeadMin (config-driven, default 30 min)
- T1: late event starts heating late (acceptable), sends Telegram "late" message (now implemented)
- Valve polling: 30s intervals, max 10-min total wait, 60s retry pause
- Hubitat switch state: `'on'`/`'off'` — never `'spa'`/`'pool'`

### Remaining
Nothing — all 17 scenarios implemented and verified (13 PASS, 4 ACCEPTABLE, 0 FAIL).

## Heartbeat
- No actionable items pending.